/**
 * Thumbnail URL lookup for the cube catalog.
 *
 * Task 07 adds this as a stub that always returns null so the palette
 * degrades gracefully to swatch display. Task 10 replaces this with
 * real logic backed by the pre-generated PNG manifest.
 */

/**
 * Returns the Vite-resolved URL for a kind's pre-generated thumbnail PNG,
 * or null if no thumbnail exists for this kind.
 *
 * Stub: always returns null until Task 10 replaces this with the real manifest.
 */
export function thumbnailUrlForKind(_id: string): string | null {
  return null;
}
